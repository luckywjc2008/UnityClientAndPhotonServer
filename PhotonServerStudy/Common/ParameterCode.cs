using System;

namespace Common
{
    public enum ParameterCode:byte
    {
        RegisterData,
        LoginData,
        Position,
        UserNameList,
        UserName,
        PlayerDataList,
    }
}
